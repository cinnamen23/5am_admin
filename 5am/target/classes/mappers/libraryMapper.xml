<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="library">

	<!-- parameterType="Integer" --> 
	<select id="getList" resultType="org.cg.domain.LibraryVO">
		select * from tbl_library
	</select>

	<select id="create" parameterType="org.cg.domain.LibraryVO">
		insert into tbl_library(lno, ltitle, lcontent, lfile) values(seq_library_lno.nextval, #{ltitle},#{lcontent},#{lfile} )
	</select>

</mapper>
  
