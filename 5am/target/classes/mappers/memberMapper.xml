<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cg.persistence.MemberDAO">

	<select id="listAlla" resultType="org.cg.domain.MemberVO" parameterType="org.cg.domain.Criteria">
		
		select * from (SELECT rownum rnum ,mid, mname,mage,maddr, mgender, regdate, updatedate FROM 
		( SELECT mid, mname,mage,maddr, mgender, regdate, updatedate FROM tbl_member where mage>0
		
		<choose>
			<when test='type=="x"'>
			 
			</when>
			<when test='type=="i"'>
			  and (mid like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="n"'>
			  and (mname like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="a"'>
			  and (mage like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="g"'>
			  and (mgender like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="d"'>
			  and (maddr like '%'|| #{keyword} ||'%')
			</when>
		</choose>
		order by regdate desc) 
		  <![CDATA[ tbl_member where rownum <= #{pageNum}) where rnum >= #{skip} ]]>
	</select>
	
	<select id="listAll" resultType="org.cg.domain.MemberVO" parameterType="hashmap">
	
	select rnum, mid, mname,mage,maddr, mgender, regdate, updatedate, cno, sid, cregdate  from 
	(SELECT rownum rnum, m.mid, mname,mage,maddr, mgender, m.regdate, m.updatedate, cno, sid, c.regdate cregdate 
	FROM TBL_MEMBER m LEFT OUTER JOIN TBL_CHOICE c ON m.mid = c.mid where sid='${vo.sid}' 
  
  		<choose>
			<when test='type=="x"'>
			 
			</when>
			<when test='type=="i"'>
			  and (mid like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="n"'>
			  and (mname like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="a"'>
			  and (mage like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="g"'>
			  and (mgender like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="d"'>
			  and (maddr like '%'|| #{cri.keyword} ||'%')
			</when>
		</choose> 
		<![CDATA[and rownum<= ${cri.pageNum} ) mc where rnum>= ${cri.skip} order by regdate desc]]>
	
	
	</select>

	<select id="totalCounta" resultType="Int" parameterType="org.cg.domain.Criteria">
		select count(*) from tbl_Member where mage>0 
		<choose>
			<when test='type=="x"'>
			 
			</when>
			<when test='type=="i"'>
			  and (mid like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="n"'>
			  and (mname like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="a"'>
			  and (mage like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="g"'>
			  and (mgender like '%'|| #{keyword} ||'%')
			</when>
			<when test='type=="d"'>
			  and (maddr like '%'|| #{keyword} ||'%')
			</when>
		</choose>
	</select>
	
	<select id="totalCount" resultType="Int" parameterType="hashmap" >
		select count(rnum) from (SELECT rownum rnum, m.mid, mname,mage,maddr, mgender, m.regdate, m.updatedate,
		 cno, sid, c.regdate cregdate FROM TBL_MEMBER m LEFT OUTER JOIN TBL_CHOICE c ON m.mid = c.mid where sid='${vo.sid}') where rnum>0
		  <choose>
			<when test='type=="x"'>
			 
			</when>
			<when test='type=="i"'>
			  and (mid like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="n"'>
			  and (mname like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="a"'>
			  and (mage like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="g"'>
			  and (mgender like '%'|| #{cri.keyword} ||'%')
			</when>
			<when test='type=="d"'>
			  and (maddr like '%'|| #{cri.keyword} ||'%')
			</when>
		</choose>
		
	</select>

	<insert id="create" parameterType="org.cg.domain.MemberVO">
		insert into	tbl_Member(mid,mpw,mname,mage,mgender,maddr) values ('${mid}','${mpw}','${mname}','${mage}','${mgender}','${maddr}')
	</insert>

	<delete id="delete" parameterType="org.cg.domain.MemberVO">
		delete from tbl_Member where mid='${mid}'

	</delete>
	
	<update id="update" parameterType="org.cg.domain.MemberVO">
		UPDATE tbl_Member SET mpw = '${mpw}', mname='${mname}',mage='${mage}',mgender='${mgender}', maddr='${maddr}', updatedate=sysdate WHERE mid = '${mid}'
	</update>
	
	<select id="qlistAll" resultType="org.cg.domain.MQuestionVO" parameterType="org.cg.domain.Criteria2">
		
		select * from (SELECT rownum rnum ,mqno, mid, sid, title, question, regdate, updatedate,checked FROM 
		( SELECT mqno, mid, sid, title, question, regdate, updatedate,checked FROM tbl_Mquestion where mqno>0
		
		<choose>
			<when test='type2=="x"'>
			 
			</when>
			<when test='type2=="i"'>
			  and (mid like '%'|| #{keyword2} ||'%')
			</when>
			<when test='type2=="t"'>
			  and (title like '%'|| #{keyword2} ||'%')
			</when>
			<when test='type2=="c"'>
			  and (question like '%'|| #{keyword2} ||'%')
			</when>
			
		</choose>
		
		<![CDATA[
		 order by regdate desc) 
		tbl_Mquestion where rownum <= #{pageNum2}) where rnum >= #{skip2}
		  ]]>
	</select>
	
	<select id="qtotalCount" resultType="Int" parameterType="org.cg.domain.Criteria2">
		select count(*) from tbl_Mquestion where mqno>0
		
		<choose>
			<when test='type2=="x"'>
			 
			</when>
			<when test='type2=="i"'>
			  and (mid like '%'|| #{keyword2} ||'%')
			</when>
			<when test='type2=="t"'>
			  and (title like '%'|| #{keyword2} ||'%')
			</when>
			<when test='type2=="c"'>
			  and (question like '%'|| #{keyword2} ||'%')
			</when>
			
		</choose>
			
	</select>
	
	<select id="qread" resultType="org.cg.domain.MQuestionVO">
		SELECT * FROM tbl_Mquestion where mqno=#{mqno}
	</select>
		
	<delete id="qdelete">
		DELETE TBL_MQUESTION where MQNO=#{mqno}
	</delete>
	
	<insert id="acreate" parameterType="org.cg.domain.MAnswerVO">
		INSERT INTO tbl_Manswer(mano, mqno, title, content) VALUES (seq_Manswer_mano.nextval, #{mqno}, '${title}', '${content}')
	</insert>
	
	<update id="achecked" parameterType="org.cg.domain.MAnswerVO">
		UPDATE TBL_MQUESTION SET checked = CHECKED+1 where MQNO=${mqno}
	</update>
	
	<select id="aread" resultType="org.cg.domain.MAnswerVO">
		SELECT * FROM tbl_Manswer where mqno=#{mqno} order by updatedate desc
	</select>
	
	<update id="aupdate" parameterType="org.cg.domain.MAnswerVO">
		UPDATE tbl_Manswer SET mqno='${mqno}', title='${title}', content='${content}' WHERE mano = '${mano}'
	</update>
	
	<delete id="adeleteall" parameterType="org.cg.domain.MQuestionVO">
		DELETE tbl_Manswer where mqno='${mqno}'
	</delete>
	
	<delete id="adelete" parameterType="org.cg.domain.MAnswerVO">
		DELETE tbl_Manswer where mano='${mano}'
	</delete>
	
	
	


</mapper>